import type { SafetyGaps } from './types';

export const DEFAULT_GAPS: SafetyGaps = { l: 3, w: 3, h: 2 };

export const DEFAULT_INNER_WALL = 3;

export const GAP_L = 3;
export const GAP_W = 3;
export const GAP_H = 2;

export const API_BASE_URL = '/api';

export const MAX_INVENTORY_ITEMS = 1000;
export const MAX_HISTORY_ITEMS = 100;
